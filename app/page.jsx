import AcademyApp from '../components/academy.jsx'

export default function Page() {
  return <AcademyApp />
}

/**
 * UI is intentionally composed from focused sections in components/academy.tsx
 * so the page remains easy to extend or connect to a CMS later.
 */
